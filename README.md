# wether-app-v0.1
A simple and responsive weather application that fetches real-time temperature, wind speed, and timezone data using the Open-Meteo API. It supports automatic location detection through the browser’s geolocation API, and also provides a manual refresh feature.
